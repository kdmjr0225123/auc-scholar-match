# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
apply-*.ps1

# Environment files — VITE_ vars here are safe to expose (they ship in the
# client bundle anyway), but this should never be the place a real secret
# lands, so it stays out of version control on principle.
.env
.env.local
.env.*.local
